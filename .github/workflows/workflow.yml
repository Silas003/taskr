name: Python CI

on:
  pull_request:
    branches: [ master, develop ]
  push:
    branches: [ master, develop, feature/* ]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
          POSTGRES_DB: taskr
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DRIVERF: postgresql+psycopg2
      DB_FUSERNAME: postgres
      DB_FPASS: password
      DB_FHOST: 127.0.0.1
      DB_FPORT: 5432
      DB_FNAME: taskr

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        run: pytest